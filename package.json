{
  "dependencies": {
    "pure-uuid": "1.5.5"
  },
  "devDependencies": {
    "http-server": "0.11.1",
    "parcel-bundler": "1.9.7",
    "rimraf": "2.6.2"
  },
  "license": "MIT",
  "main": "index.js",
  "name": "uuid-generator-website",
  "private": true,
  "scripts": {
    "build": "parcel build -d dist src/index.html",
    "clear": "rimraf dist",
    "dist": "yarn clear && yarn build",
    "start": "parcel serve --no-cache src/index.html --open",
    "test": "exit 0"
  },
  "version": "1.0.0"
}
